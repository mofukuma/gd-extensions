# 開発ルール

R01 短く書く。
R02 後方互換用の未使用コードを残さない。
R03 処理を共通化し、短く読みやすくする。
R04 file、class、関数、処理単位、設定値には目的を先に述べる日本語コメントを書く。
R05 中間 file は `tmp/` 以下に置く。
R06 Python は `uv` で実行する。
R07 `/Applications/Godot 4.7.1.app` で実動試験する。
